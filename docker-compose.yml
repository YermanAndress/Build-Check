version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      TESSDATA_PATH: /usr/share/tesseract-ocr/5/tessdata
    restart: unless-stopped
